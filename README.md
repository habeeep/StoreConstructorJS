# Store Constructor (PWA)

Minimal PWA app (React + Vite + Redux) to create a customized web store.

Run:

```bash
npm install
npm run dev
```

Notes:
- Pages: `/auth`, `/verify`, `/create`, `/stats`
- API endpoints provided in the task are used in `src/api/client.js`
